<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ajaxfire.corporates">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Rules"
            android:label="@string/title_activity_rules"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SetupPlayers" />
        <activity android:name=".PassDevice" />
        <activity android:name=".RevealRole" />
        <activity android:name=".ChooseTeam" />
        <activity android:name=".Vote" />
        <activity android:name=".VotingResult" />
        <activity android:name=".FinalScore"></activity>
    </application>

</manifest>